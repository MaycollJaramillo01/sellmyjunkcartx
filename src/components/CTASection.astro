---
interface Props { title: string; text: string; secondary?: boolean }
const { title, text, secondary = true } = Astro.props;
---
<section class="section section-compact">
  <div class="container cta-panel reveal">
    <div><h2>{title}</h2><p>{text}</p></div>
    <div class="button-row"><a class="button button-primary" href="/#quote">Get my offer</a>{secondary && <a class="button button-outline" href="tel:+13239497072" data-track="phone_clicked">Call (323) 949-7072</a>}</div>
  </div>
</section>
